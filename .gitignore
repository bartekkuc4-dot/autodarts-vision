# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── AutoDarts Vision ──────────────────────────────────────────────────────────

# Environment variables (may contain secrets / token URLs)
.env
.env.*
!.env.example

# Python virtualenv
backend/.venv
backend/__pycache__/
backend/*.pyc

# Feedback retraining dataset (large binary files — keep local)
backend/feedback/

# Model weights (large binary — distribute separately)
# Comment this line out if you want to version your best.pt in git
backend/best.pt

# Docker override files
docker-compose.override.yml
